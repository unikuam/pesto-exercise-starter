function sqrt(num) {
  return Math.sqrt(num);
}

function power(base, power) {
  return Math.pow(base, power);
}

function round(num) {
  return Math.floor(num);
}
export {
  sqrt,
  power,
  round,
};
